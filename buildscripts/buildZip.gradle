distZip {
//    dependsOn autocomplete, untunedStartScripts
    dependsOn untunedStartScripts
    doFirst {
        println 'distZip'
        delete fileTree(dir: 'build/distributions', include: '*.zip')
    }
    setVersion(archiveBuildVersion)
    setVersion(archiveBuildVersion)
}